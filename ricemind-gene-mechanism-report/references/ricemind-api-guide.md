# Extracted RiceMind API 功能说明

Source document bundled in this skill:

`references/ricemind-api-guide.docx`

Generated date in source document: 2026-06-02.

## Overall Design

RiceMind_Data is a Django REST Framework style data-service app. All APIs are `GET` requests and parameters are passed through URL query strings. Public routes are defined by `RiceMind_Data/urls.py`. Swagger UI is available at `/docs/`.

The default API base URL is controlled by `RICEMIND_API_BASE_URL`; if unset, it is `http://lit-evi.hzau.edu.cn/ricemind-api/`.

Typical request:

`http://lit-evi.hzau.edu.cn/ricemind-api/gene-profile/?gene=sd1`

Data access has three main layers:

- `Entity` / `Sentence`: literature entities and sentence retrieval.
- `UnifiedGene` / `RiceGTA_Knowledge`: gene standardization and gene-trait knowledge graph.
- `RiceGTA_SentenceEvidence` / `FullArticleSentence`: sentence-level evidence and context.
- `RiceOmicsGene` / `RiceOmicsTranscript`: sequence data.

## Route Overview

| Relative path | View | Function |
|---|---|---|
| `docs/` | `schema-swagger-ui` | Swagger UI generated by `drf_yasg`. |
| `all-genes/` | `all_genes` | Paginated gene normalization list sorted by mention count. |
| `all-traits/` | `all_traits` | Paginated phenotype/trait normalization list sorted by mention count. |
| `all-varieties/` | `all_varieties` | Paginated variety normalization list sorted by mention count. |
| `gene-profile/` | `gene_profile` | Unified gene profile, cross-database IDs, annotation, omics location, external links. |
| `traits-by-gene/` | `traits_by_gene` | Gene-associated GTA traits with source, evidence code and confidence-tier aggregation. |
| `varieties-by-gene/` | `varieties_by_gene` | Varieties co-occurring with a gene at sentence level. |
| `search-by-gene/` | `search_by_gene` | Literature sentences by gene entity. |
| `search-by-variety/` | `search_by_variety` | Literature sentences by variety entity. |
| `search-by-trait/` | `search_by_trait` | Literature sentences by trait entity. |
| `search-by-trait-and-gene/` | `search_by_trait_and_gene` | Gene-trait GTA metadata plus supporting sentences. |
| `search-by-variety-and-gene/` | `search_by_variety_and_gene` | Variety-gene co-occurrence sentences. |
| `search-by-variety-and-trait/` | `search_by_variety_and_trait` | Variety-trait co-occurrence sentences. |
| `sentence-context/` | `sentence_context` | Neighboring sentences and article metadata by PMID and sentence ID. |
| `gene-omics-sequence/` | `gene_omics_sequence` | Genomic, cDNA and protein sequences by RAP ID. |

## Core Models and Fields

| Model | Main fields | API use |
|---|---|---|
| `Sentence` | `sent_id`, `year`, `pmid`, `title`, `authors`, `journal`, `doi`, `text`, `filter_tag` | Sentence and article metadata. |
| `Entity` | `sentence`, `matched_phrase`, `normalization`, `category`, `tool`, `ontology_id`, `start_pos`, `end_pos` | NER results; `category` is `Gene`, `Phenotype`, or `Variety`. |
| `FullArticleSentence` | `pmid`, `sent_id`, `text` | Context window retrieval. |
| `UnifiedGene` | `gene_id`, `rap_id`, `primary_symbol`, `all_symbols`, `cgsnl_name`, `searchable_aliases`, `explanation`, `trait_class`, `chromosome`, `start_pos`, `end_pos`, `strand`, `other_ids`, `external_links` | Gene profile and gene resolver. |
| `RiceGTA_Knowledge` | `rap_id`, `gene_symbol`, `ontology_type`, `ontology_id`, `trait_description`, `evidence_code`, `source_db`, `article_count`, `first_mention_year`, `evidence_tier` | Macro gene-trait knowledge graph. |
| `RiceGTA_SentenceEvidence` | `relation_type`, `rap_id`, `gene_symbol`, `ontology_id`, `trait_description`, `variety_id`, `variety_name`, `pmid`, `sent_id`, `source_db`, `evidence_code` | Sentence-level GTA/GVA/TVA evidence. |
| `RiceOmicsGene` / `RiceOmicsTranscript` | `rap_id`, `genomic_seq`; `transcript_id`, `cdna_seq`, `protein_seq` | Omics sequence retrieval. |

## Gene Standardization

Gene-centered interfaces call `resolve_gene_to_primary_id(gene_query)`:

1. Exact match to `UnifiedGene.gene_id`.
2. Exact match to `primary_symbol`.
3. Match against `[UPPERCASE_ALIAS]` tokens in `searchable_aliases`.

The function returns `UnifiedGene.gene_id`. Code variables may call this value `rap_id` or `standardized_id`, but the actual return is `gene_id`; if `gene_id` is not a RAP ID and downstream tables use `rap_id`, handle this carefully.

## Common Parameters

| Parameter | Meaning |
|---|---|
| `page` | Page number. Default is usually `1`. |
| `limit` | Page size. Defaults vary: global lists `500`, single gene/variety `50`, single trait `20`, co-occurrence endpoints `10` or `15`. |
| `gene` | Gene query term, standard ID, primary symbol or alias. |
| `trait` | Trait/phenotype query term; usually fuzzy-matched with `trait_description` or phenotype normalization. |
| `variety` | Variety query term. |
| `confidence` | Used by `traits-by-gene`: `High`, `Medium`, `Low`, `All`. |
| `onto_type` | Used by `traits-by-gene`: `ALL`, `GO`, `PO`, `TO`, `CO`, `RTO`. |
| `pmid`, `sent_id`, `window` | Used by `sentence-context`. |
| `rap_id` | Used by `gene-omics-sequence`. |

